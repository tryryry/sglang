# 逐点追问记录

后续回答统一追加到本文件中，不覆盖已有记录。

## A1. `_get_new_batch_prefill_raw` 整体梳理

位置：`python/sglang/srt/managers/scheduler.py:3267`。

一句话概括：这个函数负责进行**一次 prefill 选批**：从 `waiting_queue` 中挑出当前资源允许执行的请求，
处理 cache 命中、chunk、LoRA、抢占等约束，把选中的请求物化成一个 `ScheduleBatch`，然后返回给
调度主循环。

它不执行模型 forward，但在末尾的 `new_batch.prepare_for_extend()` 中会真正构造 batch 张量、分配
request slot 和 KV slot。因此它前半段是“规划与准入”，后半段是“提交与物化”。

### 1. 它在整个调度链中的位置

调用链可以先简化为：

```text
get_next_batch_to_run()
  └─ get_new_batch_prefill()
	   ├─ 创建 PrefillDelayerSinglePassExecutor
	   ├─ _get_new_batch_prefill_raw()       ← 本文主角
	   └─ finalize()，记录本次实际 prefill batch size
```

上层的选择逻辑是：

```python
prefill_plan = self.get_new_batch_prefill(running_batch)
new_batch = prefill_plan.batch_to_run
running_batch = prefill_plan.running_batch

if new_batch is not None:
	ret = new_batch                          # 有 prefill，优先跑 prefill
else:
	running_batch = self.update_running_batch(running_batch)
	ret = running_batch                      # 没有 prefill，才回退到 decode
```

所以 `_get_new_batch_prefill_raw()` 实际掌握着一个关键决定：**这一轮能否组出 prefill batch**。
只要它返回非空 batch，正常路径下这一轮就先跑 prefill，而不是 decode。

### 2. 输入、输出与核心状态

函数签名：

```python
def _get_new_batch_prefill_raw(
	self,
	prefill_delayer_single_pass,
	running_batch,
) -> Tuple[Optional[ScheduleBatch], ScheduleBatch]:
```

两个输入：

| 输入 | 含义 |
|---|---|
| `prefill_delayer_single_pass` | 本轮 prefill 延迟决策器；可为 `None` |
| `running_batch` | 已进入稳定 decode 阶段的请求集合，也是新请求做资源预算时的背景负载 |

两个返回值：

| 返回值 | 含义 |
|---|---|
| 第一个 `Optional[ScheduleBatch]` | 本轮要执行的 prefill/extend batch；组不出来则为 `None` |
| 第二个 `ScheduleBatch` | 更新后的 running decode 状态；抢占或 mixed chunk 可能改变它 |

函数还会修改若干 scheduler 持久状态：

- `self.waiting_queue`：成功选中的请求出队；被抢占的请求重新入队。
- `self.chunked_req`：推进旧 chunk，或者登记新产生的半截长请求。
- `running_batch.batch_is_full`：记录当前运行批是否已无力接纳新请求。
- 请求自身：更新 prefix match、`extend_range`、cache lock、Mamba 状态等。

因此它不是纯函数。返回第二个 `running_batch` 也不能忽略。

### 3. 先看无附加特性的主干

暂时把 grammar、HiCache、LoRA、Mamba、SWA、dLLM、优先级抢占、mixed chunk 都看成关闭，
同时假设没有正在续跑的 `chunked_req`。此时主干可分成六步：

```text
① 快速门禁                  scheduler.py:3285-3313
		↓
② 排序 waiting_queue        scheduler.py:3315-3316
		↓
③ 创建 PrefillAdder         scheduler.py:3324-3357
		↓
④ 扫描并准入请求            scheduler.py:3376-3466
		↓
⑤ 提交队列和请求状态        scheduler.py:3468-3487
		↓
⑥ 物化 extend batch 并返回  scheduler.py:3489-3551
```

理解这条主干之后，其余分支都可以看成插在其中的额外门禁或状态维护。

#### ① 快速门禁：当前是否值得尝试组 prefill batch（3285-3313）

第一道检查：

```python
# scheduler.py:3285-3289
if (
	running_batch.batch_is_full or len(self.waiting_queue) == 0
) and self.chunked_req is None:
	return None, running_batch
```

无附加特性时 `self.chunked_req is None`，所以条件可简化成：

```text
running_batch 已标记为满  OR  waiting_queue 为空
```

- `waiting_queue` 为空：没有新请求可做 prefill，没必要继续构造预算对象。
- `running_batch.batch_is_full`：上一轮已经确认运行批没有接纳空间，直接沿用这个结论。
- 返回的第一个值是 `None`：本函数没有生成 prefill batch；上层随后可以尝试 decode。
- 第二个值仍是 `running_batch`：已有 decode 请求不能丢。

接着计算当前 decode batch size：

```python
# scheduler.py:3290
running_bs = len(running_batch.reqs)
```

它参与 request slot 和 mixed-decode token 的预算。无 mixed chunk 时，后者为 0，但 request slot
检查仍然需要它。

省略 `min_free_slots_delayer` 后，第二道主干检查是：

```python
# scheduler.py:3307-3313
if (
	self.get_num_allocatable_reqs(running_bs) <= 0
	and self.chunked_req is None
	and not self.enable_priority_preemption
):
	running_batch.batch_is_full = True
	return None, running_batch
```

`get_num_allocatable_reqs()` 的定义就在上方：

```python
# scheduler.py:3235-3238
def get_num_allocatable_reqs(self, running_bs):
	res = get_parallel().pp_max_micro_batch_size - running_bs
	res = min(res, self.req_to_token_pool.available_size())
	return res
```

也就是取两个上限的较小值：

$$
	ext{allocatable requests}
= \min(
	ext{PP microbatch limit} - \text{running batch size},
	ext{free request-pool slots}
)
$$

这里检查的是**请求槽位数量**，不是 KV token 数量：

- PP microbatch 还能容纳多少个请求。
- `req_to_token_pool` 还有多少个 request slot。

若结果不大于 0，连一个新请求的元数据槽位都没有，因此先置
`running_batch.batch_is_full = True`，再返回。KV token 是否足够则留给后面的 `PrefillAdder` 判断。

无附加特性时 `TEST_RETRACT` 为 False，所以 `3318-3322` 的测试钩子不会改变主干。

**这一步结束后的状态：**所有队列和请求都没变；只确认“有候选请求，而且至少有一个 request slot”。

#### ② 排序：决定候选请求的尝试顺序（3315-3316）

```python
# scheduler.py:3315-3316
# Get priority queue
self.policy.calc_priority(self.waiting_queue, running_batch)
```

`calc_priority()` 会**原地重排** `self.waiting_queue`。例如：

- FCFS：保持到达顺序。
- LPM：prefix cache 命中越长越靠前。
- LOF：预计输出越长越靠前。
- priority scheduling：先按显式优先级，再按等待时间。

这一步只回答：**先尝试谁？** 它不回答：**谁一定能加入 batch？**

例如排序后为：

```text
waiting_queue = [P2, P1, P3]
```

后面会依次尝试 `P2 → P1 → P3`。即使 `P2` 排第一，它仍可能因为真实 KV 成本过高而无法准入。

**这一步结束后的状态：**请求集合没变，只改变 `waiting_queue` 的遍历顺序。

#### ③ 建账：创建本轮独立的 `PrefillAdder`（3324-3357）

先准备两个预算参数：

```python
# scheduler.py:3324-3339
chunked_prefill_size = self.chunked_prefill_size

attn_backend = self.tp_worker.model_runner.attn_backend
if hasattr(attn_backend, "extend_attention_block_m"):
	prefill_tile_block_m = attn_backend.extend_attention_block_m
else:
	prefill_tile_block_m = 64
```

无 chunked prefill 时，`chunked_prefill_size` 通常是 `None`，表示请求不能在这里被切成多轮；能准入就
完整 prefill，不能准入就留在等待队列。`prefill_tile_block_m` 在非 AMD tile-budget 路径中不会形成额外
限制，但仍作为统一参数传入。

然后创建账本：

```python
# scheduler.py:3340-3357
adder = PrefillAdder(
	self.page_size,
	self.tree_cache,
	self.token_to_kv_pool_allocator,
	running_batch,
	self.new_token_ratio_tracker.current,
	self.max_prefill_tokens,
	chunked_prefill_size,
	running_bs if self.is_mixed_chunk else 0,
	self.priority_scheduling_preemption_threshold,
	max_prefill_bs=int(self.max_prefill_bs),
	max_running_requests=self.max_running_requests,
	prefill_max_requests=get_schedule().prefill_max_requests,
	prefill_delayer_single_pass=prefill_delayer_single_pass,
	dllm_config=self.dllm_config,
	waiting_queue_len=len(self.waiting_queue),
	prefill_tile_block_m=prefill_tile_block_m,
)
```

在当前简化场景中，最重要的参数是：

| 参数 | 用途 |
|---|---|
| `page_size` | 把 token 成本按 KV page 对齐，并预留页开销 |
| `tree_cache` + KV allocator | 计算可用、可驱逐的 KV token 数 |
| `running_batch` | 先扣除已有 decode 请求预计还会生成的 KV |
| `new_token_ratio` | 对未来 decode 占用做动态估算，而非一律按 `max_new_tokens` 全额预留 |
| `max_prefill_tokens` | 限制本轮 forward 的 prefill 计算量 |
| `chunked_prefill_size` | 限制本轮可处理的 chunk token；关闭时为 `None` |

`PrefillAdder` 刚创建时：

```text
adder.can_run_list = []
adder.preempt_list = []
adder.new_chunked_req = None
```

同时它已根据 allocator、tree cache 和 `running_batch` 算出本轮初始剩余预算。之后每成功加入一个请求，
这些预算都会立即扣减，所以下一个请求看到的是最新余额。

**这一步结束后的状态：**队列尚未改变；得到一个只服务于本轮选批的局部预算对象 `adder`。

#### ④ 扫描：prefix match 后逐个尝试准入（3376-3466）

无 LoRA、Mamba、PD prefill、HiCache 和优先级抢占时，循环骨架可以压缩成：

```python
# scheduler.py:3380-3465，省略附加特性分支
for req in self.waiting_queue:
	running_bs = len(running_batch.reqs)
	if len(adder.can_run_list) >= self.get_num_allocatable_reqs(running_bs):
		running_batch.batch_is_full = True

	if running_batch.batch_is_full:
		break

	req.init_next_round_input(self.tree_cache)
	res = adder.add_one_req(
		req,
		has_chunked_req=False,
		truncation_align_size=self.truncation_align_size,
	)

	if res != AddReqResult.CONTINUE:
		if res == AddReqResult.NO_TOKEN:
			running_batch.batch_is_full = True
		break
```

这个循环要分四层理解。

**④-a request slot 累计检查（3384-3398）**

阶段①只验证“开始时至少能放一个请求”。随着 `can_run_list` 增长，每轮都要重新比较：

```python
len(adder.can_run_list) >= self.get_num_allocatable_reqs(running_bs)
```

例如只有 2 个 request slots 可用：

```text
加入 P1 后：len(can_run_list) = 1 < 2，继续
加入 P2 后：len(can_run_list) = 2 >= 2，停止
```

这防止选出的请求数超过 PP microbatch 或 req pool 容量。

**④-b 刷新请求的真实 prefill 成本（3410）**

```python
req.init_next_round_input(self.tree_cache)
```

请求的原始 prompt 长度不等于本轮实际计算量。这里会根据当前 radix/tree cache 状态初始化下一轮输入，
其中关键结果包括：

- `req.prefix_indices`：已经命中、可复用的 KV 位置。
- `req.full_untruncated_fill_ids`：本轮所依据的完整输入序列。
- 随后可由两者差值得到尚需 extend 的 token 数。

核心成本近似为：

$$
	ext{extend input length}
= \lvert\text{full fill ids}\rvert - \lvert\text{prefix indices}\rvert
$$

例如 prompt 有 1000 token：

```text
cache 命中 800 token → 实际只需 extend 200 token
cache 命中   0 token → 实际需要 extend 1000 token
```

必须把这一步放在 `add_one_req()` 前，因为 cache 可能刚被别的请求更新或驱逐，不能仅用请求入队时的旧
命中结果做预算。

**④-c 多维预算准入（3428-3432）**

```python
res = adder.add_one_req(
	req,
	has_chunked_req=(self.chunked_req is not None),
	truncation_align_size=self.truncation_align_size,
)
```

在无附加特性的普通路径中，`add_one_req()` 主要做这些事：

1. 计算未命中输入、未来 decode token 和 page overhead 的总成本。
2. 检查 KV 总预算和当前 extend 峰值预算。
3. 检查 `max_prefill_tokens` 与 `prefill_max_requests`。
4. 暂时锁住命中的 cache node，避免分配时驱逐自己即将复用的前缀。
5. 设置 `req.extend_range`，把成功请求加入 `adder.can_run_list`。
6. 从 adder 的剩余预算中扣除该请求成本。

此时仍只是“选入本轮”，还没有创建 `ScheduleBatch`。

**④-d 根据返回值决定是否继续（3437-3465）**

| `res` | 当前请求 | 后续扫描 | `batch_is_full` |
|---|---|---|---|
| `CONTINUE` | 已加入 | 继续看下一个 | 不变 |
| `NO_TOKEN` | 可能已加入，也可能未加入 | `break` | 普通路径置 True |
| `OTHER` | 可能已加入，也可能未加入 | `break` | 不因本结果置 True |

之所以写“可能已加入”，是因为 `add_one_req()` 会在成功加入最后一个请求并扣完预算后，再返回当前预算
状态。因此 `NO_TOKEN/OTHER` 表示“不要继续看下一个”，不一定表示“拒绝当前请求”。源码用下面的判断
区分：

```python
# scheduler.py:3450
added = len(adder.can_run_list) > 0 and req is adder.can_run_list[-1]
```

在无 Mamba 场景，后面的回滚分支没有实际工作；在 Mamba 场景，它会释放未成功加入请求的预分配状态。

**这一步结束后的状态：**`adder.can_run_list` 已形成，但 `waiting_queue` 还没删除这些请求，真正的队列
提交统一放到下一阶段。

#### ⑤ 提交：更新 waiting queue 和请求时间（3468-3487）

先取出选批结果：

```python
# scheduler.py:3468-3471
can_run_list: List[Req] = adder.can_run_list
if len(can_run_list) == 0:
	return None, running_batch
```

若没有任何请求通过准入，本轮组批失败，第一个返回值为 `None`。注意此前 `waiting_queue` 仍保持完整，
这些请求可在后续轮次重新尝试。

若选中了请求，则统一出队：

```python
# scheduler.py:3473-3474
can_run_set = set(can_run_list)
self.waiting_queue = [x for x in self.waiting_queue if x not in can_run_set]
```

例如：

```text
排序后的 waiting_queue = [P2, P1, P3]
can_run_list             = [P2, P1]

提交后 waiting_queue     = [P3]
```

这是一次性重建列表，而不是在遍历期间删除元素，避免跳过请求或破坏迭代器。

无抢占、无 chunked request 时，`3475-3485` 不修改其他队列状态。最后记录这些请求正式进入 forward
阶段的时间：

```python
# scheduler.py:3487
set_time_batch(can_run_list, "set_forward_entry_time")
```

**这一步结束后的状态：**成功请求已从等待态转为待执行态，但仍未进入持久的 `running_batch`。

#### ⑥ 物化：创建 extend batch，实际分配资源并返回（3489-3551）

先把请求封装为 batch：

```python
# scheduler.py:3489-3499
new_batch = ScheduleBatch.init_new(
	can_run_list,
	self.req_to_token_pool,
	self.token_to_kv_pool_allocator,
	self.tree_cache,
	self.model_config,
	self.enable_overlap,
	self.spec_algorithm,
	chunked_req=self.chunked_req,
)
```

`init_new()` 建立 `ScheduleBatch` 对象并挂上请求、allocator、cache、模型配置等上下文。紧接着：

```python
# scheduler.py:3511
new_batch.prepare_for_extend()
```

`prepare_for_extend()` 才真正执行本轮 prefill 的资源和张量准备，主要包括：

1. 设置 `new_batch.forward_mode = ForwardMode.EXTEND`。
2. 从每个请求的 `prefix_indices` 和 `extend_range` 计算输入、prefix length、extend length、sequence length。
3. 分配 request pool slot 和 KV cache slot。
4. 构造 `input_ids`、`seq_lens`、`req_pool_indices`、`out_cache_loc` 等 forward 所需字段。

因此前面的 `PrefillAdder` 是“预算上认为放得下”，这里是“真正把资源分配出来”。

随后生成本轮统计：

```python
# scheduler.py:3517-3528
new_batch.prefill_stats = PrefillStats.from_adder(...)
```

在关闭 mixed chunk 时，`3531-3549` 的 `if` 条件为 False，只执行：

```python
new_batch.decoding_reqs = None
```

它明确表示这个新 batch 只包含 prefill 请求，没有混入旧 decode 请求。最后：

```python
# scheduler.py:3551
return new_batch, running_batch
```

这里两个对象的状态是：

```text
new_batch     = 本轮立即执行的 EXTEND batch，例如 [P2, P1]
running_batch = 原来持续 decode 的 batch，例如 [D1, D2]
waiting_queue = 尚未获准的请求，例如 [P3]
```

外层看到 `new_batch is not None` 后，本轮优先执行它。`P2/P1` 完成 prefill 后也不会在本函数内立即塞入
`running_batch`；它们先成为事件循环的 `last_batch`，到下一轮 `get_next_batch_to_run()` 开头才经过
filter 后合入 `running_batch`：

```text
本轮返回：
  batch_to_run = [P2, P1] EXTEND
  running_batch = [D1, D2]

本轮 forward 完成，下一轮开始：
  last_batch = [P2, P1]
  running_batch = [D1,D2] + filter(last_batch)
				= [D1,D2,P2,P1]
```

这就是为什么函数要同时返回 `new_batch` 和 `running_batch`：前者是**本轮执行对象**，后者是
**跨轮保存的 decode 状态**，二者生命周期不同。

### 4. 完整流程分成九个阶段

#### 阶段一：吸收异步就绪事件

```python
if self.grammar_manager.has_waiting_grammars():
	ready_reqs = self.grammar_manager.get_ready_grammar_requests()
	for req in ready_reqs:
		self._add_request_to_queue(req)

if self.enable_hierarchical_cache or get_memory().enable_flexkv:
	self.tree_cache.check_hicache_events()
```

这里先让外围异步状态收敛：

- grammar 编译完成的请求重新进入调度队列。
- HiCache/FlexKV 的异步加载、驱逐等事件先被消费。

否则后面的准入判断会基于过期状态。

#### 阶段二：快速判断本轮是否根本不该尝试 prefill

主要有四道快速门禁：

1. `running_batch.batch_is_full`，或 `waiting_queue` 为空。
2. `min_free_slots_delayer` 要求暂时保留 request slots。
3. `get_num_allocatable_reqs(running_bs) <= 0`，即 PP microbatch 上限或 req pool slot 已满。
4. `TEST_RETRACT` 测试模式主动禁止 prefill。

任一条件命中就直接返回：

```python
return None, running_batch
```

但 `self.chunked_req` 是重要例外。已有 chunked 请求已经占有部分状态，必须继续推进到完成，否则可能
长期悬挂资源。因此前三类正常容量门禁大多带有：

```python
and self.chunked_req is None
```

优先级抢占和 hybrid SWA 路径还会先清掉旧的 `batch_is_full`：

```python
if self.enable_priority_preemption or self.is_hybrid_swa:
	running_batch.batch_is_full = False
```

原因是这两条路径需要重新计算：前者可能通过抢占腾出空间，后者的可用空间会随 SWA 状态变化；不能
被上一轮的“满”永久挡住。

#### 阶段三：确定候选顺序和本轮预算参数

```python
self.policy.calc_priority(self.waiting_queue, running_batch)
```

这一步会原地排序 `waiting_queue`。策略只决定“谁先被尝试”，例如 FCFS、LPM、priority；它并不保证
排在前面的请求一定能运行，真正准入由后面的 `PrefillAdder` 决定。

随后确定两个本轮参数：

- `chunked_prefill_size`：静态 chunk 大小；动态 chunking 开启时可能根据历史进度重算。
- `prefill_tile_block_m`：attention backend 的 query tile 高度，AMD tile budget 准入会使用。

#### 阶段四：创建本轮 `PrefillAdder`

```python
adder = PrefillAdder(
	page_size=...,
	tree_cache=...,
	token_to_kv_pool_allocator=...,
	running_batch=...,
	new_token_ratio=...,
	rem_input_tokens=self.max_prefill_tokens,
	rem_chunk_tokens=chunked_prefill_size,
	...
)
```

`PrefillAdder` 是本轮临时的**多维预算账本和准入器**。它综合维护：

- 当前 extend 所需 KV。
- 请求未来 decode 可能需要的 KV。
- paged allocator 对齐余量。
- `max_prefill_tokens` 计算预算。
- chunk、SWA、Mamba、dLLM、tile 等专项预算。
- 请求数量、prefill delayer 和优先级抢占约束。

它的核心产物是 `adder.can_run_list`，即本轮已获准运行的请求。

#### 阶段五：旧的 `chunked_req` 无条件优先续跑

```python
if self.chunked_req is not None:
	self.chunked_req.init_next_round_input()
	self.chunked_req = adder.add_chunked_req(self.chunked_req)
```

这一段发生在遍历 `waiting_queue` 之前，所以半截长请求拥有最高优先权。

- 仍未完成：`add_chunked_req()` 返回原请求，`self.chunked_req` 继续保留。
- 本轮完成最后一段：返回 `None`，`self.chunked_req` 被清空。

这个请求会先进入 `adder.can_run_list`，后续普通请求只能使用剩余预算。

#### 阶段六：初始化批量准入所需的辅助状态

LoRA 开启时，先收集当前 running batch 和已加入 chunk 的 LoRA：

```python
running_loras = {req.lora_id for req in running_batch.reqs if not req.finished()}
running_loras.update(req.lora_id for req in adder.can_run_list)
```

这样后续请求才能判断其 adapter 是否已经在批中，或能否再加载一个。

Mamba allocator 开启时则包住整个 waiting queue 扫描：

```python
mamba_allocator.alloc_group_begin(len(self.waiting_queue))
...
mamba_allocator.alloc_group_end()
```

它让一轮内的 Mamba slot 分配按组管理，而不是每个请求独立提交。

#### 阶段七：扫描 `waiting_queue`，逐个尝试准入

这是函数的核心循环。每个请求依次经过五步。

**第一步：检查 LoRA 个体条件。**

```python
if self.enable_lora and not self._can_schedule_lora_req(req, running_loras):
	continue
```

这里用 `continue`：这个请求暂时不能进，不代表后面的请求也不能进。

**第二步：检查 request slot，并在允许时尝试优先级抢占。**

```python
if len(adder.can_run_list) >= self.get_num_allocatable_reqs(running_bs):
	running_batch.batch_is_full = True

if running_batch.batch_is_full:
	if not self.enable_priority_preemption or not adder.preempt_to_schedule(...):
		break
```

这里是全局容量限制，所以抢占失败后用 `break`，结束本轮扫描。PD disaggregation 的 prefill 侧还会
额外读取 req pool 的实时 available size，因为 prealloc/transfer queue 也可能占 slot。

**第三步：等待 HiCache 预取完成。**

```python
if not self.tree_cache.check_prefetch_progress(req.rid):
	continue
```

某个请求还在从更低层 cache 加载时，只跳过它，继续看后面的候选。

**第四步：计算请求本轮的真实输入成本。**

```python
req.init_next_round_input(self.tree_cache)
```

这一步会做 radix/cache prefix match，得到：

- 哪些 token 已有 KV，可以直接复用。
- 哪些 token 仍需 extend。
- 是否需要 host load-back、Mamba COW 等状态。

所以同样长度的两个 prompt，cache 命中不同，交给 `PrefillAdder` 的实际成本可能完全不同。
HiCache `buffer_only` 模式还会在之后补正 staged host/SWA hit 的计费信息。

**第五步：交给 `PrefillAdder` 做最终准入。**

```python
res = adder.add_one_req(req, ...)
```

返回值有三种：

| 返回值 | 含义 | 循环行为 |
|---|---|---|
| `CONTINUE` | 请求已加入，且还有预算 | 继续尝试下一个 |
| `NO_TOKEN` | KV 或专项内存预算不足 | 设置/更新 `batch_is_full`，然后停止 |
| `OTHER` | 本轮 token、chunk、请求数或 delay 等限制到达 | 不标记持久内存已满，但结束本轮 |

注意：`add_one_req()` 可能先把当前请求加入 `can_run_list`，再因为加入后预算恰好耗尽而返回
`NO_TOKEN/OTHER`。因此不能只看返回值判断请求是否已加入，代码使用对象身份判断：

```python
added = len(adder.can_run_list) > 0 and req is adder.can_run_list[-1]
```

若没加入，则回滚 `init_next_round_input()` 可能预先建立的 Mamba COW/slot 状态，避免泄漏。

#### 阶段八：提交选批结果并创建真正的 extend batch

循环结束后，若一个请求都没选中：

```python
if len(adder.can_run_list) == 0:
	return None, running_batch
```

否则开始提交状态：

1. 将 `can_run_list` 中的请求移出 `waiting_queue`。
2. 将优先级抢占产生的 `preempt_list` 重新放回队列。
3. 将 `adder.new_chunked_req` 登记为 `self.chunked_req`。
4. 增加 `inflight_middle_chunks`，用于 overlap 下追踪尚未回收的中间 chunk。
5. 记录请求进入 forward 的时间。

然后创建 batch：

```python
new_batch = ScheduleBatch.init_new(can_run_list, ..., chunked_req=self.chunked_req)
new_batch.prepare_for_extend()
```

两步的边界是：

- `ScheduleBatch.init_new()`：把请求和 scheduler 的 pool/cache/config 引用组装成 batch 对象。
- `prepare_for_extend()`：设置 `ForwardMode.EXTEND`，计算 `input_ids`、prefix/extend/seq lengths，
  分配 req/KV slots，并构造 forward 所需张量。

这意味着 `PrefillAdder` 做的是预算预测，而 `prepare_for_extend()` 是真正资源落地。若前面的预算错误，
通常会在这里表现为分配失败。

随后还会设置 HiCache consumer、prefill-aware SWA floor，并通过 `PrefillStats.from_adder()` 把 adder
累计的输入量、cache hit、重处理量等转换为本 batch 的统计数据。

#### 阶段九：可选的 mixed chunk 合并

满足以下条件时：

- 开启 `is_mixed_chunk`。
- 当前已有非空 `running_batch`。
- prefill 和 decode 都不请求 logprob。
- 新 prefill batch 不使用 `input_embeds`。

代码会把已有 decode 请求混进刚创建的 extend batch：

```python
running_batch.prepare_for_decode()
new_batch.mix_with_running(running_batch)
new_batch.decoding_reqs = running_batch.reqs
running_batch = ScheduleBatch(reqs=[], batch_is_full=...)
```

最终 GPU 一次 forward 同时处理：

- 新请求的 extend/prefill token。
- 老请求的一步 decode token。

因为老 decode 请求已经被移入 `new_batch`，返回的独立 `running_batch` 必须替换成空对象，避免同一批
请求在两个地方重复持有。未进入 mixed 模式时，`new_batch.decoding_reqs = None`，原 running batch 保留。

### 5. 四种典型返回场景

| 场景 | 第一个返回值 | 第二个返回值 |
|---|---|---|
| 没等待请求、被 delay、无 slot、没有任何请求通过准入 | `None` | 原 running batch，可能更新了 `batch_is_full` |
| 普通 prefill 成功 | 新的 extend batch | 原 running batch |
| 优先级抢占后 prefill | 新的 extend batch | 移除了被抢占请求的 running batch；被抢占请求已回等待队列 |
| mixed chunk | 含 prefill + decode 的 mixed batch | 新建的空 running batch |

还有一种贯穿多轮的状态：chunked 请求本轮返回 extend batch，但 `self.chunked_req` 仍指向该请求，所以下一轮
它会再次先于 waiting queue 续跑，直至最后一个 chunk 完成。

### 6. 最值得抓住的三个边界

#### “排序”与“准入”分离

`SchedulePolicy.calc_priority()` 只决定尝试顺序；`PrefillAdder.add_one_req()` 才决定能否加入。不要把
LPM/FCFS/priority 理解成已经生成了 batch。

#### “预测”与“分配”分离

`PrefillAdder` 先以 token/page/SWA/Mamba 等预算预测是否放得下；`prepare_for_extend()` 才实际分配。
前者是防止后者 OOM 的准入保护。

#### “新 prefill batch”与“持久 running batch”分离

普通模式下，函数返回两个不同 batch：新 batch 立即执行 prefill，running batch 留待后续 decode；只有
mixed chunk 才把两者合并，并显式清空返回的 running batch。

### 7. 一张总流程图

```text
grammar/HiCache 事件收敛
		  ↓
快速门禁：有工作吗？该 delay 吗？有 req slot 吗？
		  │ 否
		  └────────────────────────────→ return (None, running_batch)
		  ↓ 是
SchedulePolicy 原地排序 waiting_queue
		  ↓
确定 chunk size / tile block，创建 PrefillAdder
		  ↓
已有 chunked_req？──是──→ 优先续跑并先占预算
		  ↓
遍历 waiting_queue
  ├─ LoRA 不可调度 ────────────────→ continue
  ├─ req slot 满且不能抢占 ─────────→ break
  ├─ HiCache 预取未完成 ────────────→ continue
  ├─ init_next_round_input：算 prefix/cache 命中
  └─ add_one_req
	   ├─ CONTINUE ─────────────────→ 下一个
	   ├─ NO_TOKEN ─────────────────→ 标记容量压力，break
	   └─ OTHER ────────────────────→ 本轮配额结束，break
		  ↓
can_run_list 为空？──是─────────────→ return (None, running_batch)
		  ↓ 否
选中请求出队 / 被抢占请求回队 / 更新 chunked_req
		  ↓
ScheduleBatch.init_new + prepare_for_extend
		  ↓
记录 HiCache / SWA / PrefillStats
		  ↓
mixed chunk？──是──→ 把 running decode 混入 new_batch，返回空 running_batch
		  │ 否
		  └────────→ 保留原 running_batch
		  ↓
return (new_batch, running_batch)
```

最简心智模型是：

> `_get_new_batch_prefill_raw()` 是 scheduler 的“prefill 装箱与提交事务”：先依据当前运行负载、cache
> 命中和多种资源预算选人，再一次性更新队列/chunk 状态并物化 extend batch。
