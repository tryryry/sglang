# add_one_req：一个请求要满足什么条件才能被加入 prefill batch（2026-09-20）

代码位置：`python/sglang/srt/managers/schedule_policy.py:1208`
调用点：`python/sglang/srt/managers/scheduler.py:3428`（`_get_new_batch_prefill_raw` 主循环内）

相关笔记：`study/LOG_9_16` 的 A7（`_get_new_batch_prefill_raw` 详解）、A8（`PrefillAdder` 是什么）

---

## 0. 先把"被加入"定义清楚

**"被加入" = `self.can_run_list.append(req)` 这行被执行。**

全函数只有三处 append：

| 行 | 路径 | 触发条件 |
|---|---|---|
| 1371 | 整条装得下 | `chunk_tokens_limit is None or input_tokens <= chunk_tokens_limit` |
| 1421 | 装不下，切片 | 上一条不成立，且切出的 `trunc_len > 0` |
| 1359 | dLLM 专用 | `self.dllm_config is not None`（`_add_dllm_req` 内部 append） |

**返回值不等于是否被加入**——这是最大的陷阱，第 5 节单独讲。

---

## 1. 两层门禁

一个请求要走到 `add_one_req` 里面，先得过调用方（`_get_new_batch_prefill_raw` 主循环）的门：

```
waiting_queue（已被 policy.calc_priority 排序）
   │
   ├─ [调用方] LoRA 不可调度 ─────────────→ continue（跳过这个，看下一个）
   ├─ [调用方] hicache 预取未完成 ────────→ continue
   ├─ [调用方] batch_is_full 且抢占失败 ──→ break（整轮结束）
   ├─ [调用方] init_next_round_input() ──→ 算出 prefix_indices，决定这个请求的"成本"
   │
   ↓
add_one_req(req, has_chunked_req, truncation_align_size)
   └─ 九道闸，见下
```

两点值得先记住：

- **`init_next_round_input(self.tree_cache)` 是成本的决定因素**。它去 radix cache 做前缀匹配，
  命中 90% 前缀的请求真实 extend 长度只有 10%，几乎不花预算。同一个请求在不同时刻
  "能否加入"的结论可能完全不同——因为缓存状态变了。
- **`self.chunked_req` 根本不走 `add_one_req`**。它在主循环之前由 `add_chunked_req`（:1004）处理，
  无条件插队，连预算见底都硬给一份（`if _rem_tokens <= 0: _rem_tokens = self.rem_chunk_tokens`）。
  原因是半截的 chunked prefill 被卡住 = KV 内存泄漏。

---

## 2. add_one_req 的九道闸（按代码顺序）

### 闸 0 — DSA context parallel 独占（:1214）

```python
if (self.dsa_prefill_cp_in_seq_split) and len(self.can_run_list) >= 1:
    return AddReqResult.OTHER
```

开了 DSA prefill 序列内 CP 时，一批只能有一个请求（注释说当前有精度问题，暂时限死 batch=1）。

### 闸 1 — 请求数硬上限（:1217）

```python
if (x := self.prefill_max_requests) is not None and len(self.can_run_list) >= x:
    return AddReqResult.OTHER
```

对应 `--prefill-max-requests`。

### 分流 — ignore_eos 特殊路径（:1220）

```python
if req.sampling_params.ignore_eos and getattr(self.tree_cache, "disable", True):
    return self.add_one_req_ignore_eos(req)
```

`ignore_eos` 的请求一定会跑满 `max_new_tokens`，预算模型完全不同，走单独的函数（见第 4 节）。
注意条件是 `ignore_eos` **且** tree_cache 被禁用。

### 闸 2 — KV 总预算（无锁粗筛，:1243）

```python
max_new = min(max(req.sampling_params.max_new_tokens - len(req.output_ids), 0), CLIP_MAX_NEW_TOKENS)
cand_extend_input_len = len(req.full_untruncated_fill_ids) - len(req.prefix_indices)
total_tokens = cand_extend_input_len + max_new + self.page_size
total_tokens += self._mamba_gap_budget_for_req(req)

if total_tokens >= self.rem_total_tokens:
    return AddReqResult.NO_TOKEN
```

**这是最核心的一道闸**。要求：

```
未命中前缀的 extend token 数
  + 未来可能生成的 token 数（封顶 CLIP_MAX_NEW_TOKENS）
  + page_size（paged allocator 每请求最多多占一页）
  + mamba 共享槽成本（仅统一 Mamba 池，且该请求还没有 state 时）
< rem_total_tokens
```

注意是 `>=` 判失败，所以必须**严格小于**才过。

`rem_total_tokens`（:671）= `KV池可用 + tree_cache可驱逐 - rem_total_token_offset`，
其中 offset 已经扣掉了 running batch 里每个请求的未来 decode 预留：

```python
def _get_running_request_total_token_offset(self, req):       # :661
    return min(req.sampling_params.max_new_tokens - len(req.output_ids),
               CLIP_MAX_NEW_TOKENS) * self.new_token_ratio
```

所以这道闸真正在问的是：**"把这个请求收进来，从现在到它生成结束，KV 池会不会被打爆"**——
不只是这一次 prefill 放不放得下。

### 闸 3 — SWA 池预算（hybrid SWA 模型，:1247）

```python
if self.is_hybrid_swa:
    swa_needed = self._swa_budget_for_req(real_input_tokens, self._swa_new_tokens(req),
                                          swa_host_hit_length=req.swa_host_hit_length)
    if swa_needed >= self.rem_swa_tokens:
        if not self._swa_req_never_fits(...):
            return AddReqResult.NO_TOKEN          # 只是暂时挤，等一等
        swa_cap = self._swa_chunk_cap(...)        # 永远装不下 → 逃生通道
        if self.rem_chunk_tokens is None or swa_cap <= 0:
            return AddReqResult.NO_TOKEN
        chunk_tokens_limit = min(self.rem_chunk_tokens, swa_cap)
```

滑窗模型（Gemma、Mistral 等）有独立的 SWA KV 池，必须单独对账。预算公式（:727）：

```
budget = max(alloc - window, 0) + min(extend + max_new, window) + page_size + 载回charge
其中 alloc = min(extend_input_len, rem_chunk_tokens)
```

这里有个精心设计的**两级判定**：

- `swa_needed >= rem_swa_tokens` 但 `_swa_req_never_fits()` 为 **False** → 只是**当前**挤，
  等 running 请求的窗口释放就能装下 → 返回 `NO_TOKEN` 让它等。
  注释明确警告：如果这时硬塞进 decode headroom，会把 SWA 可驱逐缓冲打穿，
  引发"severe retraction/re-prefill storm"。
- `_swa_req_never_fits()` 为 **True**（预算超过整个 SWA 池容量，怎么等都装不下）→
  走 `_swa_chunk_cap()` 逃生通道，强行缩小 chunk。不这么做就是队首活锁（head-of-line livelock）。

### 闸 4 — max_prefill_tokens（仅关闭 chunked prefill 时，:1270）

```python
if (self.rem_chunk_tokens is None
    and len(self.can_run_list) != 0
    and real_input_tokens >= self.rem_input_tokens):
    return AddReqResult.OTHER
```

注释：

> - if the can_run_list is not empty, we satisfy the constraint of (max_prefill_tokens)
> - **if the can_run_list is empty, always accept the first prefill request**

`len(self.can_run_list) != 0` 这个条件就是**防饿死的豁免**：一个超长 prompt 如果每次都因
"超出 `max_prefill_tokens`" 被拒，会永远卡在队首。所以本批第一个请求无条件放行——
大不了这一批只有它一个。

### ===== 进锁 `_lock_node(req.last_node)`（:1280）=====

```python
with self._lock_node(req.last_node):
    if total_tokens >= self.rem_total_tokens:      # 闸 2 重查
        return AddReqResult.NO_TOKEN
    if self.is_hybrid_swa:
        ...                                        # 闸 3 重查
```

`_lock_node` 对匹配到的 radix 节点 `inc_lock_ref`，**防止分配新 KV 时触发 LRU 驱逐，
把自己刚匹配上的前缀驱逐掉**（自杀式驱逐）。

锁内必须重查闸 2、闸 3，因为加锁动作本身可能引发驱逐，改变 `rem_total_tokens`。
注释：`self.rem_total_tokens may decrease after the lock acquisition`。

### 闸 5 — prefill delayer 协商（:1309）

```python
if (self.prefill_delayer_single_pass is not None) and (
    not self.prefill_delayer_single_pass.negotiate_should_allow_prefill(
        local_prefillable=True,
        running_batch=self.running_batch.batch_size(),
        max_prefill_bs=self.max_prefill_bs,
        max_running_requests=self.max_running_requests,
        waiting_queue_len=self.waiting_queue_len)):
    return AddReqResult.OTHER
```

为了保护 decode 的 ITL 而主动延迟 prefill。注释说明了位置为什么在这：

> Negotiate only after every KV-budget gate (a NO_TOKEN rank must report not-prefillable
> via finalize()) and before init_load_back (a delay verdict must not start KV load-back).

在所有 KV 闸之后（DP 各 rank 要如实上报自己能不能 prefill），在 `init_load_back` 之前
（决定要延迟了就不该开始搬 KV）。

### 副作用步骤 — HiCache load-back（:1320）

```python
if req.needs_host_load_back():
    new_indices, req.last_node = self.tree_cache.init_load_back(...)
    req.prefix_indices = torch.cat([req.prefix_indices, new_indices])
    prefix_len = len(req.prefix_indices)
    req.cache_protected_len = prefix_len
```

这不是闸，是**改变状态**：把 host 上的 KV 搬回 device，`prefix_indices` 变长，
于是下面重算的 `input_tokens` 会变小。

### 闸 4' — 用更新后的 prefix 重算（:1336）

```python
input_tokens = self.ceil_paged_tokens(len(req.full_untruncated_fill_ids) - len(req.prefix_indices))
if (self.rem_chunk_tokens is None and len(self.can_run_list) != 0
    and input_tokens >= self.rem_input_tokens):
    return AddReqResult.OTHER
```

### 闸 6 — AMD prefill tile budget（:1355 / 1363 / 1412）

```python
if (tile_stop := self._check_prefill_tile_budget(input_tokens)) is not None:
    return tile_stop        # AddReqResult.OTHER
```

`_check_prefill_tile_budget`（:637）只在 **AMD（`_IS_HIP`）且 `PREFILL_TILE_BUDGET > 0`** 时生效，
且 `can_run_list` 为空时直接放行（又是"第一个无条件接受"）。它估算加上这个候选后
attention kernel 的 tile 数是否超预算。

### 闸 7 / 闸 8 — 切片路径的额外条件（:1386-1414）

走到这里说明 `input_tokens > chunk_tokens_limit`，要切片：

```python
trunc_len = chunk_tokens_limit // self.page_size * self.page_size      # 向下页对齐
if trunc_len <= 0:
    return AddReqResult.OTHER                                          # 闸 7

if truncation_align_size is not None:                                  # 闸 8
    if trunc_len < truncation_align_size:
        return AddReqResult.OTHER
    trunc_len = truncation_align_size * (trunc_len // truncation_align_size)

now_input_len = (trunc_len + len(req.prefix_indices)) // page_size * page_size
trunc_len = now_input_len - len(req.prefix_indices)
if trunc_len <= 0:
    return AddReqResult.OTHER                                          # 闸 7 再判一次
```

`truncation_align_size` 用于确定性推理（flashinfer 后端要求 prefix 长度是 attention split size 的整数倍）。
两次页对齐之后 `trunc_len` 可能被削成 0，所以要判两次。

---

## 3. 通过之后：三条出路

```python
if self.dllm_config is not None:                                   # 出路 A：dLLM
    self._add_dllm_req(req, prefix_len)
    self._req_inc_lock_ref(req)

elif chunk_tokens_limit is None or input_tokens <= chunk_tokens_limit:   # 出路 B：整条装下
    req.set_extend_range(len(req.prefix_indices), len(req.full_untruncated_fill_ids))
    self.can_run_list.append(req)
    self._req_inc_lock_ref(req)
    self._update_prefill_budget(prefix_len, input_tokens,
                                min(req.sampling_params.max_new_tokens, CLIP_MAX_NEW_TOKENS), ...)

else:                                                               # 出路 C：切片
    req.set_extend_range(len(req.prefix_indices), len(req.prefix_indices) + trunc_len)
    self.can_run_list.append(req)
    self.new_chunked_req = req                 # ★ 这就是 self.chunked_req 的诞生地
    self._req_inc_lock_ref(req)
    self._update_prefill_budget(prefix_len, trunc_len, 0, ...)      # max_new 传 0
```

三点差异：

1. **出路 B 的 `_update_prefill_budget` 传真实的 `max_new_tokens`，出路 C 传 0**。
   因为切片的请求这一轮不会生成任何 token（还没喂完 prompt），不需要为它预留 decode 空间。
2. **出路 C 会设 `self.new_chunked_req = req`**，被调用方在 3479-3482 取走变成 `self.chunked_req`。
3. 三条路都调 `_req_inc_lock_ref(req)`——把前缀节点的引用计数持久化（不同于 `_lock_node` 的临时锁）。

`_update_prefill_budget`（:864）立即扣减所有预算，所以**下一个请求看到的是扣减后的余额**：

```python
self.rem_total_token_offset += extend_input_len + max_new_tokens + page_overhead + mamba_gap_reserve
self.cur_rem_token_offset   += extend_input_len +                  page_overhead + mamba_gap_reserve
self.rem_input_tokens       -= extend_input_len
if self.rem_chunk_tokens is not None:
    self.rem_chunk_tokens   -= extend_input_len
if mamba_gap_reserve and self.rem_mamba_slots is not None:
    self.rem_mamba_slots    -= 1
```

---

## 4. 特殊路径：`add_one_req_ignore_eos`（:1072）

`ignore_eos` 的请求不会提前停，一定跑满 `max_new_tokens`，所以乐观预留会失效。这条路径用
**全局可行性检查**代替单请求预算：

```python
paged_input = self.ceil_paged_tokens(cand_extend_input_len) + self._mamba_gap_budget_for_req(req)
if paged_input > min(self.cur_rem_tokens, self.rem_total_tokens):
    return AddReqResult.NO_TOKEN
```

然后构造 `req_states`（每个请求的 `(tokens_left, tokens_occupied)`，按 `tokens_left` 排序），
模拟"按结束先后顺序逐个释放"的过程，检查任一时刻是否会耗尽 KV：

```python
cur_rem_tokens = self.cur_rem_tokens - self.ceil_paged_tokens(cand_extend_input_len)
tokens_freed = 0
for i, (tokens_left, tokens_occupied) in enumerate(self.req_states):
    bs = len(self.req_states) - i                      # 此刻还活着的请求数
    min_free_tokens = cur_rem_tokens + tokens_freed - tokens_left * bs
    if min_free_tokens <= IGNORE_EOS_RESERVE_TOKENS * bs:
        return AddReqResult.NO_TOKEN
    tokens_freed += tokens_occupied
```

注意 `is_hybrid_swa` 时跳过这段模拟——注释说 SWA 的内存管理不同，这个机制会低估用量。

---

## 5. 陷阱：返回值 ≠ 是否被加入

`add_one_req` 最后一行是：

```python
return self.budget_state()      # :1435
```

`budget_state()`（:841）报的是**"预算还够不够继续收下一个"**，不是"这个请求收没收"。

所以完全可能出现：**请求已经 append 进 `can_run_list`，但返回 `NO_TOKEN`**——
因为正是这次 `_update_prefill_budget` 把预算扣到了 0。

调用方因此不能用返回值判断，只能用身份比较（`scheduler.py:3450`）：

```python
added = len(adder.can_run_list) > 0 and req is adder.can_run_list[-1]
if not added:
    # 回收 init_next_round_input 里已经分配的 mamba slot，否则泄漏
    req.mamba_cow_src_index = None
    req.mamba_needs_clear = False
    if req.mamba_pool_idx is not None and not getattr(req, "session", None):
        self.tree_cache.req_to_token_pool.mamba_allocator.free(req.mamba_pool_idx.unsqueeze(-1))
        req.mamba_pool_idx = None
```

四种组合都真实存在：

| 是否 append | 返回值 | 含义 |
|---|---|---|
| ✅ | `CONTINUE` | 收了，还能继续收 |
| ✅ | `NO_TOKEN` | 收了，但 KV 预算被这次收人扣光了 |
| ✅ | `OTHER` | 收了，但 `max_prefill_tokens` / chunk 预算用完了 |
| ❌ | `NO_TOKEN` / `OTHER` | 没收，闸中途 return |

（不存在"没 append 却返回 CONTINUE"，因为提前 return 的分支只返回 `NO_TOKEN` / `OTHER`。）

---

## 6. 条件汇总表

一个**普通请求**（非 dLLM、非 ignore_eos）要被加入，必须同时满足：

| # | 条件 | 不满足时 | 行 |
|---|---|---|---|
| 1 | 非 DSA-CP-in-seq-split 模式下的第二个请求 | `OTHER` | 1214 |
| 2 | `len(can_run_list) < prefill_max_requests` | `OTHER` | 1217 |
| 3 | `extend + max_new + page + mamba_gap < rem_total_tokens` | `NO_TOKEN` | 1243 |
| 4 | hybrid SWA 时 `swa_needed < rem_swa_tokens`（或触发 chunk cap 逃生） | `NO_TOKEN` | 1256 |
| 5 | 关闭 chunked prefill 时 `input_tokens < rem_input_tokens`（**本批第一个豁免**） | `OTHER` | 1270 |
| 6 | 加锁后条件 3、4 仍成立 | `NO_TOKEN` | 1282 |
| 7 | prefill delayer 放行 | `OTHER` | 1309 |
| 8 | AMD tile budget 未超（**本批第一个豁免**） | `OTHER` | 1355+ |
| 9 | 走切片路径时 `trunc_len > 0` 且满足 `truncation_align_size` | `OTHER` | 1390+ |

外加**隐含条件**（不在 `add_one_req` 里，但决定它能否被调用到）：

| # | 条件 | 位置 |
|---|---|---|
| 10 | LoRA adapter 可加载 / drainer 放行 | `scheduler.py:3381` |
| 11 | hicache 预取已完成 | `scheduler.py:3400` |
| 12 | `batch_is_full` 为 False，或优先级抢占成功 | `scheduler.py:3393` |
| 13 | 该请求在 `waiting_queue` 里排得够前（`policy.calc_priority` 的结果） | `scheduler.py:3316` |

---

## 7. 判定流程图

```
add_one_req(req)
  │
  ├─ DSA-CP 已有请求? ─────────────────────────────→ OTHER
  ├─ can_run_list 满 (prefill_max_requests)? ──────→ OTHER
  ├─ ignore_eos && tree_cache.disable? ────────────→ add_one_req_ignore_eos（另一套逻辑）
  │
  ├─ total = extend + max_new + page + mamba_gap
  │  total >= rem_total_tokens? ───────────────────→ NO_TOKEN
  │
  ├─ hybrid SWA && swa_needed >= rem_swa?
  │     ├─ 只是暂时挤 ──────────────────────────────→ NO_TOKEN（等）
  │     └─ 永远装不下 ──→ chunk_tokens_limit = swa_cap（缩小 chunk 继续）
  │
  ├─ 无 chunked prefill && 非首个 && 超 max_prefill_tokens? ─→ OTHER
  │
  ├─── with _lock_node(req.last_node):  ← 钉住前缀，防自杀式驱逐
  │     ├─ 重查 rem_total_tokens / rem_swa_tokens ─→ NO_TOKEN
  │     ├─ prefill delayer 否决? ───────────────────→ OTHER
  │     ├─ needs_host_load_back? ──→ init_load_back（prefix 变长，成本下降）
  │     ├─ 重查 max_prefill_tokens ────────────────→ OTHER
  │     │
  │     ├─ input_tokens <= chunk_limit?
  │     │     ├─ 是 → tile budget 超? ─→ OTHER
  │     │     │       否 → set_extend_range(全长) + append ✅
  │     │     └─ 否 → trunc_len = 页对齐(chunk_limit)
  │     │             trunc_len <= 0 或不满足 align? ─→ OTHER
  │     │             tile budget 超? ─→ OTHER
  │     │             set_extend_range(trunc_len) + append + new_chunked_req ✅
  │     │
  │     └─ _update_prefill_budget()  ← 立即扣减，下一个请求看到扣减后的余额
  │
  └─ return budget_state()    ← 报"还能不能继续"，不是"这个收没收"
```

---

## 8. `total_tokens` 和 `input_tokens` 的区别

其实是**四个**量，不是两个。

| 变量 | 行 | 公式 | 维度 | 用在哪 |
|---|---|---|---|---|
| `cand_extend_input_len` | 1230 | `len(full_untruncated_fill_ids) - len(prefix_indices)` | 原料 | 派生下面三个 |
| `total_tokens` | 1233 | `cand + max_new + page_size + mamba_gap` | **显存** | 闸 2 / 闸 2'（KV 总预算） |
| `real_input_tokens` | 1239 | `ceil_page(cand - host_hit_length)` | **算力** | 闸 4（锁前的 max_prefill_tokens） |
| `input_tokens` | 1332 | `ceil_page(len(fill_ids) - len(prefix_indices))`，锁内重算 | **算力** | 闸 4'、tile budget、切片判定、扣预算 |

一句话区别：

> **`total_tokens` 回答"要占多少 KV 格子"，`input_tokens` 回答"要跑多少 token 的 forward"。**

### 差异 1：加不加 `max_new_tokens`

`total_tokens` 加，`input_tokens` 不加。

KV 池要为这个请求**未来生成**的 token 留位置（否则下一步 decode 就 OOM 触发 retract），
而 forward 算力只关心**这一次**要算多少——未来的 token 是未来 decode 步的事。

这就是 `rem_total_tokens` 和 `rem_input_tokens` 两本账的源头（见 LOG_9_16 的 A7 §②）。

### 差异 2：减不减 `host_hit_length` ← 最容易搞错的一个

```python
total_tokens      = cand_extend_input_len + max_new + self.page_size        # 不减
real_input_tokens = cand_extend_input_len - req.host_hit_length             # 减
real_input_tokens = self.ceil_paged_tokens(real_input_tokens)
# 注释：adjusting the input_tokens based on host_hit_length and page_size
```

`host_hit_length` 是 HiCache 在 **host 内存（L2）** 命中的 token 数。这些 token：

- **需要占 device KV** —— `init_load_back` 会把它们从 host 搬回 device，一个格子都不能少
  → 所以 `total_tokens` **不减**
- **不需要重新计算** —— 搬回来就行，不过 attention
  → 所以 `input_tokens` **要减**

**搬运 ≠ 计算**，这是两个维度分开算的最直接体现。

如果 `total_tokens` 也减掉 host_hit，就会低估显存需求，载回的那一刻可能直接 OOM。

### 差异 3：页对齐的方式不同

```python
total_tokens      = cand_extend_input_len + max_new + self.page_size   # 直接加一页
real_input_tokens = self.ceil_paged_tokens(real_input_tokens)          # 向上取整到页边界
```

- `total_tokens` **加一个 `page_size`** 当余量。`_update_prefill_budget` 里的注释解释了为什么：
  `alloc_extend reserves an extra page_size per request to make sure the budget doesn't over-commit`
- `input_tokens` **向上取整**：`ceil_paged_tokens(t) = -(-t // page_size) * page_size`

前者是固定余量（按最坏情况留一页），后者是实际会分配的页数。

### `input_tokens` 为什么要算两次

锁前（1239）和锁内（1332）各算一次，中间夹着 `init_load_back`：

```python
real_input_tokens = ceil_page(cand_extend_input_len - req.host_hit_length)   # 1239 锁前，预估

with self._lock_node(req.last_node):
    ...
    if req.needs_host_load_back():
        new_indices, req.last_node = self.tree_cache.init_load_back(...)
        req.prefix_indices = torch.cat([req.prefix_indices, new_indices])    # ← prefix 变长了！
        prefix_len = len(req.prefix_indices)

    input_tokens = ceil_page(len(req.full_untruncated_fill_ids) - len(req.prefix_indices))  # 1332 重算
```

`init_load_back` **真的把 host 上的 KV 搬回了 device 并并入 `prefix_indices`**，
所以载回的那段从"要算的"变成了"已经在 prefix 里的"。重算之后 `input_tokens ≈ real_input_tokens`
（载回成功时两者相等）。

区别在于：

- `real_input_tokens` 是**预估** —— "假设载回会成功，我要算这么多"
- `input_tokens` 是**实测** —— "载回已经做完，`prefix_indices` 是真实的，现在要算这么多"

所以锁内那次重算不是冗余，是**用事实替换预估**。后续所有决策（切片判定、tile budget、扣预算）
都用 `input_tokens`，只有锁前的粗筛用 `real_input_tokens`。

### 扣预算时两个维度又合流

`_update_prefill_budget(prefix_len, input_tokens, max_new, ...)` 传进去的是 `input_tokens`，
内部再按两个维度分头扣：

```python
extend_input_len = self.ceil_paged_tokens(extend_input_len)    # 已对齐，幂等
page_overhead = self.page_size

self.rem_total_token_offset += extend_input_len + max_new_tokens + page_overhead + mamba_gap  # 显存
self.cur_rem_token_offset   += extend_input_len +                  page_overhead + mamba_gap  # 显存峰值
self.rem_input_tokens       -= extend_input_len                                               # 算力
if self.rem_chunk_tokens is not None:
    self.rem_chunk_tokens   -= extend_input_len                                               # 切片
```

注意 `rem_total_token_offset` 的扣减公式和 `total_tokens` 的组成**完全一致**
（extend + max_new + page + mamba_gap），只是 extend 那一项从闸门用的 `cand_extend_input_len`
换成了实际的 `input_tokens`。于是形成一个刻意的保守偏差：

```
闸门检查用：cand_extend_input_len   （含 host_hit，未页对齐）  ← 偏大
实际扣减用：input_tokens            （已减 host_hit，已页对齐）← 偏小
```

闸门用的数偏大，宁可拒绝也不冒险放行——正好对应 `_update_prefill_budget` 那句 TODO 的自我评价：
`may be too conservative`。

### 一个数字例子

prompt 1000 token，radix device 命中 200，HiCache host 命中 300，`max_new_tokens=128`，`page_size=64`：

```
cand_extend_input_len = 1000 - 200 = 800      # device 上没有的部分
host_hit_length       = 300                    # 其中 300 在 host，可以搬不用算

total_tokens      = 800 + 128 + 64 = 992                        # 显存：800 个格子都要占（含要搬的 300）
real_input_tokens = ceil_page(800 - 300) = ceil_page(500) = 512 # 算力：只算 500，对齐到 512

  → 闸 2 检查 992 < rem_total_tokens ?
  → 闸 4 检查 512 < rem_input_tokens ?

load_back 之后 prefix_indices 从 200 涨到 500：
input_tokens = ceil_page(1000 - 500) = 512                      # 与 real_input_tokens 一致

  → 扣预算：rem_total_token_offset += 512 + 128 + 64 = 704      （比闸门用的 992 小）
            rem_input_tokens      -= 512
```

同一个请求，显存维度记 992（闸门）/ 704（扣减），算力维度记 512。三个数都对，因为问的是不同的问题。

---

## 9. 三句话总结

1. **能否加入取决于四类预算同时够用**：KV 总量（含未来 decode 预留）、SWA 池、
   `max_prefill_tokens` 算力、`chunked_prefill_size` 切片——外加 Mamba 槽、dLLM block、
   AMD tile 这些条件性约束。任何一类见底就拒。
2. **装不下不一定被拒，可能被切片**。这是 chunked prefill 的本质：把"要么全收要么不收"
   变成"收多少算多少"，代价是产生一个必须在后续轮次优先续上的 `chunked_req`。
3. **两处"第一个无条件接受"的豁免是防饿死的**（闸 5 和闸 8 的 `can_run_list` 为空判断）。
   没有它们，超长 prompt 会永远卡在队首。
